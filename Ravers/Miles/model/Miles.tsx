/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import * as THREE from 'three';
import React, { useRef, useEffect, useState } from 'react';
import { useGLTF } from '@react-three/drei';
import { useFrame } from '@react-three/fiber';
import { GLTF } from 'three/examples/jsm/loaders/GLTFLoader';
import { AnimationMixer, Object3D } from 'three';
import { SkeletonUtils } from '../../utils/SkeletonUtils';

type GLTFResult = GLTF & {
  nodes: {
    MILES_TSHIRT_Baked: THREE.SkinnedMesh;
    MILES_Armature001: Object3D;
    mixamorigHips: THREE.Bone;
  };
  materials: {
    ['MILES TSHIRT_Bake1_baked']: THREE.MeshStandardMaterial;
  };
};

type ActionName = 'miles bboy uprock_MILES Armature.001';
type GLTFActions = Record<ActionName, THREE.AnimationAction>;

const MODEL_URL =
  'https://ravers-models.s3.us-east-2.amazonaws.com/RAVERz_AVATARzMILESCOMPRESS.glb';
export default function MilesModel(props: JSX.IntrinsicElements['group']) {
  const group = useRef<THREE.Group>();
  const { nodes, materials, animations } = useGLTF(MODEL_URL) as GLTFResult;

  Object.values(materials).forEach((material) => (material.skinning = true));
  const actions = useRef<GLTFActions>();
  const [mixer, setMixer] = useState<AnimationMixer | undefined>();

  useEffect(() => {
    const model = SkeletonUtils.clone(nodes.MILES_Armature001) as Object3D;

    model.traverse((obj) => (obj.frustumCulled = false));
    const _mixer = new AnimationMixer(model);

    // @ts-ignore
    actions.current = animations.reduce((acc, cur) => {
      const name = cur.name as ActionName;
      // @ts-ignore
      acc[name] = _mixer.clipAction(cur, group.current);
      return acc;
    }, {});

    // @ts-ignore
    actions.current['miles bboy uprock_MILES Armature.001'].play();

    setMixer(_mixer);
  }, []);

  useFrame((_, delta) => {
    mixer && mixer.update(delta);
  });

  return (
    <group ref={group} {...props} dispose={null}>
      <group rotation={[0, 0, 0.1]} frustumCulled={false}>
        <group
          rotation-x={Math.PI / 2}
          scale={0.00515}
          position-y={0}
          frustumCulled={false}
        >
          <primitive object={nodes.mixamorigHips} />
          <skinnedMesh
            frustumCulled={false}
            geometry={nodes.MILES_TSHIRT_Baked.geometry}
            material={materials['MILES TSHIRT_Bake1_baked']}
            skeleton={nodes.MILES_TSHIRT_Baked.skeleton}
          />
        </group>
      </group>
    </group>
  );
}

useGLTF.preload(MODEL_URL);
